# haoyouping-config

好有评配置下发仓库，存放 `config.json` 配置文件。

## 仓库结构

- `config.json` 主配置文件
- `README.md` 仓库说明

## CLI 工具

使用 `hyp-cli` 管理本仓库：

```
hyp-cli status            # 查看状态
hyp-cli announcement add  # 新增公告
hyp-cli release           # 发布新版本
hyp-cli push -m "..."     # 推送变更
```
